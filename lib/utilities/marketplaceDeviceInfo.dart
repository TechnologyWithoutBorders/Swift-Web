import 'hospitalDevice.dart';

class MarketplaceDeviceInfo {
  final HospitalDevice device;
  final String imageData;
  final String location;
  final int distance;

  MarketplaceDeviceInfo({this.device, this.imageData, this.location, this.distance});
}